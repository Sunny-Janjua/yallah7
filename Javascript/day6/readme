A function in programming is a reusable block of code that performs a specific task or a set of tasks. It can take input in the form of parameters, process that input, and return an output. Functions help organize code, making it more modular, readable, and easier to maintain.